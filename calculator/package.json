{
	"name": "calculator",
	"version": "1.0.0",
	"description": "create calculator with electron",
	"main": "main.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"packager": "electron-packager ./ myApp --out ./bin-release --overwrite --icon=./favicon.ico --ignore=node_modules",
		"start": "electron ."
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"electron": "^9.4.0"
	}
}